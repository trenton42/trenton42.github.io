# Hugo build!
version: 2
jobs:
  build:
    branches:
      ignore:
        - master
        - test
    docker:
      # specify the version
      - image: monachus/hugo
    working_directory: /site
    steps:
      - checkout

      # specify any bash command here prefixed with `run: `
      - run: hugo
